<Window x:Class="Talisman.PomodoroSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pomodoro Day Summary"
        WindowStyle="ToolWindow"
        Topmost="True"
        ResizeMode="CanResize"
        Width="380" Height="440">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBox x:Name="SummaryText" Grid.Row="0" IsReadOnly="True" TextWrapping="Wrap"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="12" Background="#FFFAFAFA" />
        <Button Grid.Row="1" Content="Close" Click="CloseClicked" Width="90"
                HorizontalAlignment="Right" Margin="0,8,0,0" />
    </Grid>
</Window>
