<UserControl x:Class="Talisman.TimerDetailsWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Talisman"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="600">
    <Grid Background="LightGray">
        <StackPanel Orientation="Horizontal">
            <Label Width="70" Content="{Binding EndsAtText}" HorizontalContentAlignment="Right"  />
            <Button Content="×" FontFamily="Wingdings" Click="AdjustSmallerClick" />
            <Label Content="¡" FontFamily="Wingdings" VerticalContentAlignment="Center" />
            <Button Content="Ø" FontFamily="Wingdings" Click="AdjustLargerClick" />
            <TextBox Text="{Binding Name, Mode=TwoWay}" Margin="20,0,0,0" MinWidth="250"  />
            
        </StackPanel>
    </Grid>
</UserControl>
