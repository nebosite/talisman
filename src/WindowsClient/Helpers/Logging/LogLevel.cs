namespace Talisman
{
    // --------------------------------------------------------------------------
    /// <summary>
    /// Severity levels for log messages, in increasing order of importance.
    /// </summary>
    // --------------------------------------------------------------------------
    public enum LogLevel
    {
        Debug = 0,
        Info = 1,
        Warn = 2,
        Error = 3,
        Fatal = 4,
    }
}
